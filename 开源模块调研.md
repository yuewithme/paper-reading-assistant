# 论文辅助研读助手：开源模块调研与选型

调研日期：2026-07-27

## 1. 结论

第一版不需要从零实现 PDF 解析、OCR、PDF 查看器、双栏拖拽和右键菜单。建议采用以下组合：

| 能力 | 第一选择 | 结论 |
|---|---|---|
| PDF/OCR/版面解析 | [PaddleOCR / PP-StructureV3](https://github.com/PaddlePaddle/PaddleOCR) | 第一候选，先做真实论文验证 |
| 学术元数据和参考文献 | [GROBID](https://github.com/grobidOrg/grobid) | 可选增强，不阻塞第一版 |
| 原始 PDF 查看和定位 | [PDF.js](https://github.com/mozilla/pdf.js) | 直接采用 |
| 双栏宽度拖拽 | [react-resizable-panels](https://github.com/bvaughn/react-resizable-panels) | 直接采用 |
| 右键菜单和问答抽屉 | [Radix Primitives](https://github.com/radix-ui/primitives) | 直接采用 |
| 长论文虚拟渲染 | [TanStack Virtual](https://github.com/TanStack/virtual) | 需要时采用 |
| 英文词形归一 | [spaCy](https://github.com/explosion/spaCy) | 直接采用轻量英文模型 |
| 本地 API | [FastAPI](https://github.com/fastapi/fastapi) | 直接采用 |
| 本地数据 | SQLite | 直接采用 |
| 语义向量检索 | [sqlite-vec](https://github.com/asg017/sqlite-vec) | 暂缓，第一版先保留适配接口 |

推荐的核心路径：

```text
PDF
  ↓
PaddleOCR / PP-StructureV3
  ↓
结构化 JSON + Markdown + 页面坐标
  ↓
应用内部统一 Document 模型
  ↓
语义分组、翻译、深度解读
  ↓
React 双栏阅读界面
```

关键原则：

1. 不直接让前端依赖 PaddleOCR 的原始输出格式。
2. 后端建立统一的 `DocumentParser` 接口和内部文档模型。
3. 第一版只接入 PaddleOCR，保留后续切换 Docling 或其他解析器的能力。
4. 开发开始前，用真实论文对 PaddleOCR 做小规模验证，不能只看官方演示。

## 2. PDF、OCR 和论文结构解析

### 2.1 PaddleOCR / PP-StructureV3

仓库：[PaddlePaddle/PaddleOCR](https://github.com/PaddlePaddle/PaddleOCR)

许可证：Apache-2.0。

可直接利用的能力：

- 英文和多语言 OCR；
- 扫描 PDF 识别；
- 页面方向和版面分析；
- 标题、正文、图片、表格、公式等区域识别；
- 阅读顺序恢复；
- 表格结构识别；
- 公式识别；
- Markdown 输出；
- JSON 输出；
- 文字、表格单元格等细粒度坐标；
- Windows 本地部署；
- CPU、GPU 和多种推理后端。

与本项目的适配程度很高：

- JSON 可以转换为内部段落和语义区块；
- 坐标可以用于“跳回原始 PDF”；
- Markdown 可以作为解析结果检查和降级展示；
- OCR、表格和公式能力集中在同一个官方项目中，减少拼装成本。

第一版推荐使用 PP-StructureV3，而不是已经停止重点维护的 PP-StructureV2。

建议只安装文档解析需要的依赖组，不默认安装所有功能。开发时锁定经过验证的 PaddlePaddle、PaddleOCR 和模型版本，不能始终跟随最新版。

风险：

- 安装包和模型较大；
- 首次运行需要下载模型；
- Windows、CPU/GPU 和 PaddlePaddle 版本组合需要实际验证；
- 复杂双栏、跨页表格和密集公式仍可能识别错误；
- 官方仓库更新速度快，版本升级可能产生兼容性变化。

验证要求：

1. 原生文字型英文论文。
2. 双栏排版英文论文。
3. 扫描型英文论文。
4. 包含复杂公式的论文。
5. 包含表格和跨页内容的论文。

每篇论文重点检查：

- 阅读顺序；
- 段落边界；
- 标题层级；
- 公式和表格；
- 图片及图注；
- 页面坐标；
- OCR 错字率；
- 处理时间和内存。

结论：第一版解析/OCR 的首选，但必须先通过真实样本验证。

### 2.2 Docling

仓库：[docling-project/docling](https://github.com/docling-project/docling)

许可证：MIT；具体模型还需分别检查模型许可证。

能力：

- PDF 版面和阅读顺序；
- 表格、代码、公式和图片分类；
- 扫描 PDF OCR；
- 统一的 `DoclingDocument` 数据模型；
- Markdown、HTML 和无损 JSON 输出；
- Windows、Linux 和 macOS 本地运行；
- 可作为 Python 包、CLI 或独立服务使用。

优点：

- 文档对象设计成熟；
- API 和导出格式适合接入 AI 工作流；
- 许可证宽松；
- 可以作为 PaddleOCR 的对照和后备解析器。

不作为第一版主引擎的原因：

- 与 PaddleOCR 的版面/OCR能力重叠；
- 同时接入两个大型解析框架会提高安装、调试和数据映射成本；
- 项目文档当前仍把标题、作者、参考文献等元数据提取列为后续能力。

结论：保留适配接口，并在 PaddleOCR 效果不足时作为第一后备候选。

### 2.3 MinerU

仓库：[opendatalab/MinerU](https://github.com/opendatalab/MinerU)

许可证：MinerU Open Source License，基于 Apache-2.0，但包含附加条件，不等同于标准 Apache-2.0。

能力：

- PDF 和图片转 Markdown/JSON；
- 多栏和复杂版面阅读顺序；
- 图片、表格、公式和脚注；
- 扫描件和乱码 PDF 自动 OCR；
- 公式转 LaTeX；
- 表格转 HTML；
- CLI、API、FastAPI 和 WebUI；
- CPU、GPU 和多种后端。

优势：

- 对科学论文和复杂排版针对性强；
- 输出形式与本项目需要非常接近；
- 内置完整服务和可视化工具。

不作为默认依赖的原因：

- 许可证带附加条件，需要逐条评估；
- 完整环境资源占用和模型体积较大；
- 与 PaddleOCR 能力高度重叠；
- 对个人桌面 Web 应用来说偏重。

结论：作为效果基准和后备方案，不直接写入第一版依赖。

### 2.4 GROBID

仓库：[grobidOrg/grobid](https://github.com/grobidOrg/grobid)

许可证：Apache-2.0。

擅长：

- 学术论文标题、摘要、作者和单位；
- 章节和正文结构；
- 参考文献解析；
- 正文引用与参考文献对应；
- DOI、PMID 等标识；
- PDF 坐标；
- TEI XML 输出。

它与 PaddleOCR 的关系不是替代，而是互补：

```text
PaddleOCR：版面、OCR、表格、公式、坐标
GROBID：学术元数据、引用、参考文献关系
```

成本：

- 主要为 Java 服务；
- 本地部署通常增加一个进程或 Docker 服务；
- 对扫描件仍需要 OCR 前置处理；
- 第一版的核心阅读体验不依赖完整参考文献解析。

结论：先不作为第一版硬依赖。论文库元数据或引用定位质量不足时再接入。

### 2.5 PyMuPDF

仓库：[pymupdf/PyMuPDF](https://github.com/pymupdf/PyMuPDF)

许可证：AGPL-3.0，另提供商业许可。

原计划曾建议使用 PyMuPDF。调研后不再把它作为默认依赖：

- 仅个人私用通常问题较小；
- 一旦公开发布、提供网络服务或闭源分发，就需要认真遵守 AGPL 或购买商业许可；
- 本项目未来可能从个人工具发展为可发布产品，没有必要现在引入许可证负担。

替代方式：

- 主解析交给 PaddleOCR；
- 浏览器显示交给 PDF.js；
- 简单 PDF 元数据和文件操作使用 pypdf；
- 如后端必须渲染页面，再评估 pypdfium2。

结论：第一版排除。

## 3. 前端阅读模块

### 3.1 PDF.js

仓库：[mozilla/pdf.js](https://github.com/mozilla/pdf.js)

许可证：Apache-2.0。

用途：

- 查看原始 PDF；
- 根据页码定位；
- 渲染页面缩略图；
- 将解析段落定位回原始页面；
- 必要时显示原 PDF 文字层。

不让 PDF.js 承担：

- 论文结构理解；
- OCR；
- 语义分组；
- 双语重排。

结论：直接采用 `pdfjs-dist`，主要服务于原始页查看和定位。

### 3.2 react-resizable-panels

仓库：[bvaughn/react-resizable-panels](https://github.com/bvaughn/react-resizable-panels)

许可证：MIT。

用途：

- 左侧原文/翻译与右侧 AI 解读的可拖拽宽度；
- 保存用户上次使用的左右宽度；
- AI 问答打开和关闭时控制右栏布局；
- 支持键盘和无障碍的分隔条。

结论：直接采用，避免自己处理拖拽、最小宽度和布局持久化。

### 3.3 Radix Primitives

仓库：[radix-ui/primitives](https://github.com/radix-ui/primitives)

许可证：MIT。

计划使用：

- `ContextMenu`：划词后的右键菜单；
- `Dialog` 或 `Popover`：词汇详情；
- `ScrollArea`：词汇栏和 AI 问答历史；
- `Tooltip`：页码、状态和按钮提示；
- `DropdownMenu`：论文操作；
- `AlertDialog`：删除论文确认。

Radix 只提供交互和无障碍基础，不强迫使用特定视觉风格，适合个人定制界面。

结论：直接采用。

### 3.4 TanStack Virtual

仓库：[TanStack/virtual](https://github.com/TanStack/virtual)

许可证：MIT。

用途：

- 对几十页至上百页论文进行虚拟渲染；
- 避免一次性把全部原文、翻译和 AI 解读挂到 DOM；
- 支持动态高度语义区块。

风险：

- 动态高度、左右对齐和滚动定位结合后需要仔细处理；
- 过早使用会增加阅读页调试难度。

结论：阅读页先按普通分块渲染实现；样本论文出现明显性能问题时再接入。

### 3.5 Web Highlighter

仓库：[alienzhou/web-highlighter](https://github.com/alienzhou/web-highlighter)

许可证：MIT。

能力：

- 网页文字范围高亮；
- 保存和恢复高亮；
- 基于 Selection API；
- 无运行时依赖。

它适合“用户选中一段文字并保存该范围”，但本项目的词汇高亮还有额外要求：

- 一个收藏词需要在全文所有位置高亮；
- 需要支持词形变化；
- 原文会按稳定段落 ID 渲染；
- 翻译和 AI 解读会异步更新。

因此：

- 可以用于快速原型；
- 不直接把它作为词汇系统的数据模型；
- 正式实现优先保存 `paragraph_id + start_offset + end_offset + selected_text`；
- 全文词汇高亮由词形归一和段落渲染器负责。

结论：可参考或用于原型，不作为必须依赖。

## 4. 词汇与文本处理

### 4.1 spaCy

仓库：[explosion/spaCy](https://github.com/explosion/spaCy)

许可证：MIT；安装的语言模型需要单独记录版本和许可证。

用途：

- 英文分词；
- 词形还原；
- 判断收藏内容的词元；
- 匹配常见单复数、时态和派生形式；
- 辅助区分单词和多词短语。

第一版只需要轻量英文模型，不启用自动术语提取。

需要注意：

- 用户收藏什么词完全由用户决定；
- spaCy 只负责归一和匹配；
- 不把专有名词的大小写和连字符信息随意丢失；
- 多词短语优先使用精确匹配，再做有限的词形归一。

结论：直接采用。

## 5. 后端与检索

### 5.1 FastAPI

仓库：[fastapi/fastapi](https://github.com/fastapi/fastapi)

许可证：MIT。

用途：

- PDF 导入和处理任务 API；
- PaddleOCR 调用；
- 翻译和深度解读任务；
- AI 问答；
- 本地文件和数据访问；
- 自动生成 OpenAPI 文档。

结论：直接采用。

### 5.2 SQLite

用途：

- 论文信息；
- 段落和语义区块；
- 翻译和 AI 解读缓存；
- 个人词汇；
- 阅读进度；
- 问答记录；
- AI 提示词和解析器版本。

第一版先使用 SQLite 内置 FTS5 做关键词检索。

### 5.3 sqlite-vec

仓库：[asg017/sqlite-vec](https://github.com/asg017/sqlite-vec)

许可证：MIT 或 Apache-2.0。

优点：

- 与 SQLite 保持同一个数据文件；
- Python 和 Node.js 都有绑定；
- 适合个人本地应用；
- 可以存储和检索论文段落向量。

风险：

- 项目仍明确标记为 pre-v1，可能有破坏性更新；
- 一篇论文的规模较小，第一版不一定需要专门向量索引。

结论：

- 第一版用 FTS5 加少量段落嵌入的普通表即可；
- 数据模型预留 embedding 字段；
- 论文数量增加、检索变慢后再接入 sqlite-vec。

## 6. 可参考但不直接复用的完整项目

### 6.1 Translate for Zotero

仓库：[windingwind/zotero-pdf-translate](https://github.com/windingwind/zotero-pdf-translate)

值得参考：

- 划选翻译；
- 词典和翻译服务适配层；
- 右侧翻译面板；
- 选中文字加入笔记；
- 快捷键和右键菜单。

不直接复用：

- 它与 Zotero 插件 API 深度绑定；
- 许可证为 AGPL；
- 交互核心是翻译，不是语义对齐的深度 AI 解读。

### 6.2 PDFMathTranslate

仓库：[PDFMathTranslate/PDFMathTranslate](https://github.com/PDFMathTranslate/PDFMathTranslate)

值得参考：

- 科学论文翻译；
- 公式和排版保护；
- 多翻译服务适配；
- CLI、GUI、Docker 和 Zotero 集成。

不直接作为核心：

- 主要目标是生成保留版式的翻译 PDF；
- 本项目需要的是可交互、可重排、可提问的阅读流；
- 其旧版和新版仓库、内核及许可证需要分别确认；
- 即使接入，也不能解决右侧语义解读和个人词汇系统。

### 6.3 MinerU 和 Docling 的完整管线

两者都可以快速搭建“PDF 转结构化内容”的原型。正式架构中应通过统一解析器接口接入，不能让应用数据结构直接绑定其中任意一个。

## 7. 推荐依赖清单

### 第一版直接采用

后端：

```text
FastAPI
SQLite
PaddleOCR / PP-StructureV3
spaCy
pypdf（仅简单 PDF 操作和元数据）
```

前端：

```text
React
TypeScript
pdfjs-dist
react-resizable-panels
Radix Primitives
```

按需接入：

```text
TanStack Virtual
web-highlighter
sqlite-vec
GROBID
```

仅用于比较或后备：

```text
Docling
MinerU
PDFMathTranslate
Translate for Zotero
```

明确排除：

```text
PyMuPDF（除非未来决定遵守 AGPL 或购买商业许可）
```

## 8. 解析器接口建议

内部不要使用 PP-StructureV3 的原始字段作为业务数据结构。

建议定义：

```python
class DocumentParser:
    def parse(self, pdf_path: str) -> ParsedDocument:
        ...
```

统一输出至少包含：

```text
ParsedDocument
├── metadata
├── pages
│   ├── width
│   ├── height
│   └── blocks
├── sections
├── paragraphs
├── figures
├── tables
├── formulas
└── references
```

每个内容块至少包含：

```text
id
type
page_number
bbox
reading_order
source_text
source_image
parent_section_id
parser_name
parser_version
confidence
```

这样以后可以实现：

```text
PaddleStructureParser
DoclingParser
MinerUParser
```

并且不会修改翻译、AI 解读、词汇和前端阅读逻辑。

## 9. 开发前验证任务

在正式开发完整页面前，先做一个独立解析验证：

1. 固定 PaddleOCR 和 PaddlePaddle 版本。
2. 安装 PP-StructureV3 文档解析依赖。
3. 准备 5 篇代表性英文论文。
4. 输出每篇论文的 JSON、Markdown 和版面可视化结果。
5. 记录处理时间、峰值内存和输出目录大小。
6. 人工检查阅读顺序、段落、公式、表格和坐标。
7. 选其中最困难的 2 篇，用 Docling 再跑一次作为对照。
8. 根据结果决定：
   - PaddleOCR 单独作为第一版解析器；
   - PaddleOCR 主解析 + Docling 降级；
   - 或调整内部文档模型。

验证通过标准：

- 普通论文的正文阅读顺序基本正确；
- 双栏论文不会频繁串栏；
- 扫描论文的正文可以稳定识别；
- 段落能定位回原始 PDF 页面；
- 公式和表格至少能够以结构化结果或原图保留；
- 输出足以支撑左侧重排阅读，而不是只能生成不可编辑图片。

## 10. 最终选型

当前建议：

> 以 PaddleOCR 的 PP-StructureV3 作为第一版 PDF/OCR/版面解析引擎，PDF.js 负责原始页查看，React 负责重排阅读，FastAPI 和 SQLite 负责本地服务与数据；前端直接复用 react-resizable-panels 和 Radix Primitives；词形匹配使用 spaCy。Docling 作为解析效果的首要对照方案，GROBID作为未来学术元数据增强。

这个方案最大限度复用成熟模块，同时把真正需要自行开发的部分集中在本产品独有的能力上：

- 原文、翻译与语义解读的数据组织；
- 左右语义区块对齐；
- 深度解读提示词和生成策略；
- 个人词汇与全文高亮；
- 自动上下文的右键 AI 问答；
- 阅读进度和本地缓存。
