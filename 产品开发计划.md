# 论文辅助研读助手：第一版开发计划

## 1. 产品定义

面向个人使用的英文论文精读工具。

核心体验是：

> 导入英文 PDF 后，在左侧同步阅读原文与中文翻译，在右侧直接查看按语义组织的深度 AI 解读；遇到不理解的内容时，划选文字并通过右键进入 AI 问答；遇到生词时，主动收藏并在全文中持续高亮。

产品不是通用聊天机器人，也不是简单的 PDF 翻译器。第一版重点解决三个问题：

1. 英文论文阅读速度慢。
2. 翻译能看懂，但不容易理解作者的论证结构和真实意图。
3. 生词和重要表达在阅读过程中反复出现，却缺少连续积累。

## 2. 已确认的产品原则

### 2.1 使用方式

- 第一版为电脑端 Web 应用。
- 主要为个人使用，不设计复杂的权限、协作和团队功能。
- 默认允许调用在线 AI 服务。
- 论文原文件、解析结果、阅读进度、词汇和问答记录保存在本地。

### 2.2 阅读布局

- 页面默认采用双栏布局。
- 左栏展示论文原文和中文翻译。
- 每段英文原文下方紧跟对应的中文翻译。
- 右栏展示深度 AI 解读。
- 左右内容按照语义区块对齐，而不是强制一个自然段对应一个 AI 解读。
- 一个 AI 解读可以对应左侧连续的多个自然段。
- 原文、翻译和 AI 解读默认展开，不需要点击后才生成或显示。

### 2.3 AI 解读

- 默认就是深度解读，不设置“快速解读”“展开深度解读”等层级。
- AI 根据语义决定解读范围和长度，不使用死板的一段一解读。
- AI 解读以帮助理解为主，可包含：
  - 内容的通俗解释；
  - 作者的论证逻辑；
  - 与前后文的关系；
  - 专业概念和长难句；
  - 方法、实验、公式或图表的含义；
  - 隐含前提、容易误解的地方和必要的局限分析。
- AI 不强制输出固定模板。简单内容简要解释，关键内容深入说明。
- 必须区分：
  - 论文明确表达的内容；
  - AI 根据论文作出的推断或评价；
  - AI 补充的外部背景知识。

### 2.4 词汇

- 第一版不自动提取高频词或核心术语。
- 词汇完全由用户主动划选并收藏。
- 支持收藏单词和多词短语。
- 收藏时记录该词在当前论文语境下的中文含义。
- 收藏后在全文中高亮相同词汇及合理的词形变化。
- 词汇数据按论文保存，同时形成个人总词库。

### 2.5 AI 问答

- 用户可以在原文、翻译或 AI 解读中划选文字。
- 在选中文字后，通过右键菜单选择“问 AI”。
- AI 问答窗口临时覆盖右侧解读栏，不改变左侧正文宽度。
- 系统自动使用以下内容作为上下文：
  - 用户选中的文字；
  - 文字所属的语义区块；
  - 前后相关段落；
  - 整篇论文中与问题相关的内容。
- 界面不要求用户手动选择“当前段落、当前章节、全文”等上下文范围。
- 连续追问自动继承当前对话。
- 每篇论文保存独立的问答记录。
- 关闭问答后恢复右栏 AI 解读及原来的滚动位置。

## 3. 第一版范围

### 3.1 必须完成

1. 论文库。
2. PDF 导入与文件管理。
3. 原生 PDF 文字提取。
4. 扫描型 PDF 的 OCR 识别。
5. 标题、章节、段落、图片、表格、公式等基础结构识别。
6. 可重新排版的论文阅读流。
7. 原文与中文翻译逐段对应。
8. 按语义分组的深度 AI 解读。
9. 原文、翻译和解读的双栏对齐。
10. 右键划词翻译和收藏。
11. 收藏词汇的全文高亮。
12. 右键选文进入 AI 问答。
13. 每篇论文的阅读进度保存。
14. 翻译、解读、词汇和问答结果缓存。
15. 原始 PDF 页码和解析内容之间的定位关系。

### 3.2 第一版明确不做

- 自动提取高频词或核心术语。
- 读后考试。
- 费曼式复述和 AI 评价。
- 艾宾浩斯卡片复习。
- 多人协作、分享和评论。
- 手机端适配。
- 复杂账号和权限体系。
- 多篇论文之间的知识图谱。
- 自动生成综述。

这些内容保留为后续版本规划，但不影响第一版的架构扩展。

## 4. 核心页面

### 4.1 论文库首页

展示：

- 论文标题；
- 作者和年份；
- 导入时间；
- 当前处理状态；
- 上次阅读位置；
- 阅读进度；
- 已收藏词汇数量。

支持：

- 导入 PDF；
- 打开论文；
- 删除论文及其本地数据；
- 重新解析失败的论文；
- 查看解析、翻译和解读进度。

### 4.2 论文阅读页

页面结构：

```text
┌──────────────────────────────────────────────────────────────┐
│ 论文标题 / 当前章节 / 页码 / 处理状态 / 返回论文库           │
├───────────────────────────────┬──────────────────────────────┤
│ 原文 + 中文翻译               │ 深度 AI 解读                 │
│                               │                              │
│ 语义区块 1                    │ 对应语义区块 1 的解读        │
│                               │                              │
│ 语义区块 2                    │ 对应语义区块 2 的解读        │
│                               │                              │
├───────────────────────────────┴──────────────────────────────┤
│ 词汇入口 / 当前阅读进度 / 处理提示                            │
└──────────────────────────────────────────────────────────────┘
```

主要行为：

- 页面打开后直接展示已生成内容。
- 当前可见区域尚未处理完成时，显示局部生成状态。
- 后续内容在后台继续翻译和解读。
- 鼠标经过某个语义区块时，左右对应区域同时轻微高亮。
- 点击页码或定位入口，可查看对应的原始 PDF 页面。
- 重新打开论文时恢复上次滚动位置。

### 4.3 AI 问答状态

右键选择“问 AI”后：

```text
┌───────────────────────────────┬──────────────────────────────┐
│ 原文 + 中文翻译               │ AI 问答                      │
│                               │                              │
│ 阅读位置保持不变              │ 选中文字的简短引用           │
│                               │ 问答历史                     │
│                               │ 多行输入框                   │
└───────────────────────────────┴──────────────────────────────┘
```

要求：

- 输入框自动获得焦点。
- 顶部只显示选中文字的简短引用，不显示复杂上下文设置。
- AI 回答尽可能附带页码、段落、图表或公式定位。
- 点击回答中的论文依据，可以定位并高亮左侧对应内容。
- 关闭问答后恢复原来的 AI 解读状态。

### 4.4 个人词汇栏

词汇栏可从阅读页面右侧边缘或底部固定入口打开。

每条词汇至少保存：

- 原始单词或短语；
- 当前语境下的翻译；
- 原始例句；
- 所属论文；
- 页码和段落位置；
- 收藏时间；
- 用户备注；
- 掌握状态。

第一版掌握状态可以只保留：

- 陌生；
- 学习中；
- 已掌握。

## 5. PDF 处理流程

### 5.1 导入

1. 用户选择 PDF。
2. 计算文件标识，避免重复导入。
3. 保存原始文件。
4. 创建论文处理任务。
5. 进入论文阅读页并展示处理进度。

### 5.2 文本与版面提取

优先读取 PDF 原生文字层。

每个内容块保留：

- 页码；
- 页面坐标；
- 内容类型；
- 原始文字；
- 阅读顺序；
- 与原始 PDF 的定位信息。

如果页面不存在有效文字层，或提取结果明显异常，则对该页启用 OCR，而不是默认对整篇论文进行 OCR。

### 5.3 内容结构化

将页面内容整理为统一结构：

- 论文标题；
- 作者；
- 摘要；
- 章节和小节标题；
- 正文段落；
- 列表；
- 公式；
- 图片与图注；
- 表格与表注；
- 脚注；
- 参考文献。

第一版允许复杂表格和公式保留原始图片，同时保存周围文字及页面位置。

### 5.4 语义分组

AI 根据连续段落的逻辑关系生成语义区块。

分组规则：

- 不跨越明显的章节边界。
- 通常覆盖 1 至 4 个自然段。
- 定义、关键结论和独立论点可以单独成组。
- 方法步骤、实验描述和结果解释可以合并相邻段落。
- 每个语义区块保存其对应的原始段落编号。
- 用户看到的左右对齐以语义区块为最小单位。

### 5.5 翻译与解读

按语义区块提交翻译和 AI 解读任务。

处理顺序：

1. 当前打开位置附近的内容；
2. 摘要和引言；
3. 当前章节剩余内容；
4. 论文其他章节。

结果生成后立即缓存，避免重复调用。

## 6. AI 输出约束

### 6.1 翻译

- 保持学术语义准确。
- 不擅自简化、补充或修改作者结论。
- 全文术语翻译尽量一致。
- 首次出现的重要术语保留英文。
- 保留公式编号、图表编号、引用编号和变量名。
- 无法确定的专有名词可以保留原文。

### 6.2 深度解读

- 不重复翻译已经表达清楚的内容。
- 优先解释作者为什么要写这部分，以及它如何参与整篇论文的论证。
- 根据内容决定解读重点，不强制使用固定栏目。
- 对方法说明输入、处理过程和输出。
- 对实验说明比较对象、指标、结果和结论是否匹配。
- 对公式说明变量、直观含义和使用目的。
- 对图表说明读图方式、关键趋势和能够支持的结论。
- 对不确定信息使用谨慎表达。
- AI 自己的评价必须与论文原意区分。

### 6.3 问答

- 回答优先依据当前论文。
- 需要使用外部常识时，明确标记为补充说明。
- 尽可能引用论文页码、段落、章节、图表或公式。
- 找不到论文依据时直接说明，不伪造引用。
- 用户使用“这里”“这个方法”等指代表达时，优先解释为当前选择内容或当前阅读位置。

## 7. 建议技术架构

第一版建议采用本地运行的前后端架构。

### 7.1 前端

- React + TypeScript。
- 桌面端响应式双栏布局。
- 使用 `react-resizable-panels` 实现左右栏宽度调整和布局持久化。
- 使用 Radix Primitives 实现右键菜单、问答抽屉和基础交互组件。
- 阅读内容采用结构化 HTML 渲染。
- 使用 PDF.js 提供原始 PDF 页面查看和定位。
- 自定义阅读区域右键菜单。
- 先使用分段渲染支持长论文，出现性能问题后接入 TanStack Virtual。

### 7.2 后端

- Python FastAPI。
- 使用 PaddleOCR 的 PP-StructureV3 作为第一版 PDF、OCR 和版面解析候选。
- 使用其 JSON、Markdown 和细粒度坐标结果构建应用内部的统一文档模型。
- 通过 `DocumentParser` 接口隔离具体解析器，保留后续接入 Docling 的能力。
- 不默认使用 AGPL 许可的 PyMuPDF；简单 PDF 操作使用 pypdf，原始页显示使用 PDF.js。
- 使用 spaCy 完成英文词形归一和收藏词汇匹配，不进行自动术语提取。
- 负责论文解析、语义分组、翻译、AI 解读和问答检索。
- 使用后台任务队列处理长时间任务。

### 7.3 数据与文件

- SQLite 保存结构化数据。
- 原始 PDF、页面图片和缓存文件保存在本地目录。
- 每个段落和语义区块使用稳定 ID。
- 保存段落与 PDF 页码、页面坐标之间的映射。
- AI 调用结果以输入内容和提示词版本生成缓存标识。
- 第一版使用 SQLite FTS5 检索论文内容，并为后续向量检索预留数据字段。

### 7.4 AI 接口

- 第一版使用在线模型 API。
- 翻译、语义分组、深度解读和问答分别使用独立提示词。
- 模型和密钥通过本地设置页面配置。
- AI 接口封装成可替换适配层，为后续切换模型或本地模型保留空间。

## 8. 核心数据对象

### Paper

- id
- title
- authors
- year
- file_path
- file_hash
- page_count
- processing_status
- last_read_position
- created_at
- updated_at

### DocumentBlock

- id
- paper_id
- page_number
- block_type
- reading_order
- source_text
- bbox
- parent_section_id

### Paragraph

- id
- paper_id
- block_id
- paragraph_index
- source_text
- translated_text
- page_number
- source_bbox

### SemanticGroup

- id
- paper_id
- paragraph_ids
- section_id
- analysis_text
- analysis_status
- prompt_version

### VocabularyItem

- id
- normalized_text
- display_text
- contextual_translation
- source_sentence
- paper_id
- paragraph_id
- page_number
- mastery_status
- note
- created_at

### Conversation

- id
- paper_id
- title
- created_at
- updated_at

### Message

- id
- conversation_id
- role
- content
- selected_text
- source_paragraph_ids
- citations
- created_at

## 9. 开发阶段

### 阶段 0：工程基础

目标：

- 建立前后端项目。
- 建立本地数据库和文件目录。
- 完成基础设置和日志。

验收：

- 前后端可以本地启动。
- 可以保存和读取一条测试论文记录。
- AI 密钥不写入代码仓库。

### 阶段 0.5：解析引擎验证

目标：

- 用真实论文验证 PaddleOCR PP-StructureV3，而不是直接依据官方演示进入完整开发。
- 确认内部统一文档模型能够承接解析结果。
- 用 Docling 对最困难的样本进行对照。

验收：

- 完成原生文字、双栏、扫描、公式密集和表格密集共 5 类论文测试。
- 保存每篇论文的 JSON、Markdown 和版面可视化输出。
- 记录处理时间、内存、模型体积和主要识别错误。
- 普通论文阅读顺序、段落和页面坐标能够支撑重排阅读。
- 明确决定使用 PaddleOCR 单引擎，还是增加 Docling 降级路径。

### 阶段 1：PDF 导入与结构化

目标：

- 完成论文库。
- 支持导入原生 PDF 和扫描型 PDF。
- 提取文字、页码、段落和基础版面信息。

验收：

- 原生 PDF 能按正确阅读顺序提取主要正文。
- 扫描页能够自动触发 OCR。
- 每个段落都能定位回原始 PDF 页。
- 重新打开应用后论文仍然存在。

### 阶段 2：双语阅读

目标：

- 实现论文阅读页。
- 生成逐段翻译。
- 展示原文和译文。

验收：

- 英文段落和对应翻译不会错位。
- 图表、公式和标题不会被当作普通正文错误翻译。
- 术语翻译在同一篇论文中基本一致。
- 刷新页面后已生成翻译直接从缓存读取。

### 阶段 3：语义分组与深度解读

目标：

- 将自然段组合成语义区块。
- 为每个语义区块生成深度解读。
- 实现左右区块对齐。

验收：

- 一个解读能够合理对应一个或多个自然段。
- 不跨章节错误合并。
- 页面默认显示完整解读。
- 鼠标经过时左右对应区块能够同步提示。
- AI 推断与论文明确内容在表达上能够区分。

### 阶段 4：词汇收藏与全文高亮

目标：

- 实现自定义右键菜单。
- 支持划词翻译、收藏和查看词汇。
- 支持全文高亮。

验收：

- 系统不会自动生成词汇条目。
- 用户可以收藏单词和短语。
- 词汇保存当前语境释义和原始例句。
- 再次打开论文后高亮仍然存在。
- 合理识别常见的单复数和时态变化。

### 阶段 5：AI 问答

目标：

- 右键选文进入 AI 问答。
- 自动构建论文上下文。
- 保存每篇论文的问答记录。

验收：

- 原文、翻译和解读中的文字均可作为提问入口。
- 不需要用户手动设置上下文范围。
- 连续追问能够保持对话语义。
- 回答可以定位到论文依据。
- 关闭问答后恢复解读和阅读位置。

### 阶段 6：稳定性与体验优化

目标：

- 优化长论文加载和后台处理。
- 完善失败重试、错误提示和缓存。
- 完成第一版视觉和交互细节。

验收：

- 导入、解析、OCR、翻译或 AI 调用失败时可以重试。
- 长论文滚动保持流畅。
- 后台生成不会阻塞已完成内容的阅读。
- 应用重启后阅读进度、词汇、解读和问答记录均可恢复。

### 阶段 7：统一 PaddleOCR 文档解析

目标：

- 所有 PDF 都通过 PaddleOCR PP-StructureV3 解析。
- 恢复多栏阅读顺序，并保留标题、正文、表格、公式和页面坐标。

验收：

- 不再依据 PDF 原生文本层选择解析器。
- 每个解析结果都明确标记为 OCR 结果。
- Windows CPU 环境和后续 Linux 云服务器均有可复现的依赖锁定。

### 阶段 8：自动翻译与深度解读

目标：

- OCR 完成后自动调用 Qwen 翻译与深度解读。
- AI 失败后可以单独续跑，不重复执行 OCR。

验收：

- 导入后无需手动点击生成按钮。
- 已完成的逐段翻译和语义解读可缓存复用。
- 缺少 Key 或调用失败时保留 OCR 结果，配置恢复后可继续生成。

## 10. 第一版整体完成标准

使用一篇包含正文、图片、表格和公式的英文论文，以及一篇扫描型英文论文进行完整验收。

第一版完成需要同时满足：

1. 两篇论文均可成功导入。
2. 所有论文均通过 PaddleOCR 恢复版面结构和阅读顺序。
3. 正文可以重新排版并定位回原始 PDF。
4. 左侧逐段显示原文和翻译。
5. 右侧默认显示按语义组织的深度解读。
6. 左右语义区块对应关系清晰。
7. 系统不自动提取词汇。
8. 用户主动收藏的词汇能够保存并全文高亮。
9. 划选任意阅读内容后，可以通过右键进入 AI 问答。
10. 问答不需要手动配置上下文，并能提供论文依据。
11. 关闭并重新打开应用后，所有个人数据和阅读进度仍然存在。

## 11. 主要风险与应对

### PDF 版面复杂

风险：双栏论文、脚注、公式和表格可能造成阅读顺序错误。

应对：

- 保留页面坐标和原始 PDF 定位。
- 第一版优先保证正文阅读顺序。
- 复杂公式和表格允许保留原图。
- 准备不同出版社的测试论文作为固定样本。

### AI 解读过长导致左右留白

风险：右侧解读比左侧内容长，破坏连续阅读。

应对：

- 允许深度解读，但要求避免重复翻译和无关扩展。
- 语义区块大小与解读长度共同控制。
- 左右区块按较高一侧确定高度。

### 术语翻译不一致

风险：同一术语在不同段落被翻译成不同中文。

应对：

- 为每篇论文维护翻译术语上下文。
- 后续翻译携带已使用的重要术语映射。

### AI 问答引用不可靠

风险：AI 把外部知识或推断说成论文结论。

应对：

- 问答前先检索论文原文。
- 回答保存引用段落 ID。
- 明确区分论文依据与补充解释。
- 找不到依据时要求模型直接说明。

### 在线 AI 成本和等待时间

风险：整篇翻译和深度解读调用量较大。

应对：

- 优先处理当前可见区域。
- 所有生成结果缓存。
- 按语义区块增量生成。
- 允许暂停和继续后台任务。

## 12. 后续版本方向

第一版稳定后再依次评估：

1. 艾宾浩斯词汇卡片复习。
2. 读完论文后的内容测试。
3. 费曼式复述与 AI 评价。
4. 多篇论文共享术语库。
5. 跨论文搜索与知识关联。
6. 方法、实验和结论的结构化摘要。
7. 语音提问和口头复述。
